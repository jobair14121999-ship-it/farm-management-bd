"use strict";

/*
 * Copyright (c) 2016-present Invertase Limited & Contributors
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this library except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 */

import * as firebaseFirestorePipelines from '@react-native-firebase/app/dist/module/internal/web/firebaseFirestorePipelines';
let nestedWebPipelineBuilder = null;
let nestedWebPipelineFirestore = null;
export function configureWebNestedPipelineRevival(firestore, builder) {
  nestedWebPipelineFirestore = firestore;
  nestedWebPipelineBuilder = builder;
}
function extractNestedPipelineFromArg(arg) {
  if (!isRecord(arg)) {
    return null;
  }
  if (arg.exprType === 'PipelineValue' && isRecord(arg.pipeline)) {
    return arg.pipeline;
  }
  if (isRecord(arg.source) && Array.isArray(arg.stages)) {
    return arg;
  }
  return null;
}
const WEB_PIPELINE_HELPER_ALIASES = {
  lower: 'toLower',
  upper: 'toUpper'
};
function isRecord(value) {
  return value !== null && typeof value === 'object' && !Array.isArray(value);
}
function getPipelineHelper(name) {
  const helperModule = firebaseFirestorePipelines;
  const helper = helperModule[name] ?? helperModule[WEB_PIPELINE_HELPER_ALIASES[name] ?? ''];
  if (typeof helper !== 'function') {
    throw new Error(`pipelineExecute() cannot rebuild "${name}" because the web helper is missing.`);
  }
  return helper;
}
function isExpressionNode(value) {
  return value.__kind === 'expression' || value.exprType === 'Field' || value.exprType === 'Constant' || value.exprType === 'Variable' || value.exprType === 'Function' || value.exprType === 'PipelineValue';
}
function isFlatAliasedFieldNode(value) {
  const alias = value.alias ?? value.as;
  return typeof value.path === 'string' && value.path.length > 0 && typeof alias === 'string' && alias.length > 0;
}
function isAliasedExpressionNode(value) {
  const alias = value.alias ?? value.as;
  return typeof alias === 'string' && alias.length > 0 && Object.prototype.hasOwnProperty.call(value, 'expr');
}
function isAliasedAggregateNode(value) {
  return typeof value.alias === 'string' && value.alias.length > 0 && Object.prototype.hasOwnProperty.call(value, 'aggregate');
}
const ORDERING_COMPARISON_FUNCTIONS = new Set(['greaterThan', 'greaterThanOrEqual', 'lessThan', 'lessThanOrEqual']);
const BOOLEAN_COMPARISON_FUNCTIONS = new Set(['equal', 'notEqual', 'greaterThan', 'greaterThanOrEqual', 'lessThan', 'lessThanOrEqual', 'arrayContains', 'arrayContainsAny', 'arrayContainsAll', 'equalAny', 'notEqualAny']);
const ARITHMETIC_FUNCTIONS = new Set(['add', 'subtract', 'multiply', 'divide', 'mod', 'pow']);
function getArgReviveMode(helperName, argIndex) {
  if (ARITHMETIC_FUNCTIONS.has(helperName) && argIndex > 0) {
    return 'numericOperand';
  }
  if (BOOLEAN_COMPARISON_FUNCTIONS.has(helperName) && argIndex === 1) {
    return ORDERING_COMPARISON_FUNCTIONS.has(helperName) ? 'numericOperand' : 'comparisonOperand';
  }
  return 'helper';
}
function coerceNumericOperandScalar(value) {
  if (typeof value === 'boolean') {
    return value ? 1 : 0;
  }
  if (typeof value === 'string') {
    const trimmed = value.trim();
    if (trimmed.length > 0 && Number.isFinite(Number(trimmed))) {
      return Number(trimmed);
    }
  }
  return value;
}
function isZeroNumericOperand(value) {
  if (value === 0 || value === false) {
    return true;
  }
  if (typeof value === 'string' && value.trim() === '0') {
    return true;
  }
  return false;
}
function finalizeArithmeticArgs(helperName, revivedArgs) {
  if (!ARITHMETIC_FUNCTIONS.has(helperName)) {
    return revivedArgs;
  }
  const output = revivedArgs.slice();
  for (let index = 1; index < output.length; index++) {
    output[index] = coerceNumericOperandScalar(output[index]);
  }
  return output;
}
function rebuildFlatAliasedField(value) {
  const alias = value.alias ?? value.as;
  const field = getPipelineHelper('field')(value.path);
  const asFn = field.as;
  if (typeof asFn !== 'function') {
    throw new Error('pipelineExecute() failed to rebuild flat aliased field for web SDK.');
  }
  return asFn.call(field, alias);
}
function applyAlias(revivedValue, alias, nodeKey) {
  if (!revivedValue || typeof revivedValue !== 'object' && typeof revivedValue !== 'function' || !('as' in revivedValue)) {
    throw new Error(`pipelineExecute() failed to rebuild aliased ${nodeKey}: invalid value.`);
  }
  return revivedValue.as(alias);
}
function rebuildExpressionNode(stack, node, mode, resolve) {
  if (node.exprType === 'Field' || typeof node.path === 'string') {
    resolve(getPipelineHelper('field')(node.path));
    return;
  }
  if (node.exprType === 'Constant' || Object.prototype.hasOwnProperty.call(node, 'value')) {
    if (mode === 'numericOperand') {
      if (typeof node.value === 'boolean') {
        resolve(coerceNumericOperandScalar(node.value));
        return;
      }
      stack.push({
        kind: 'evaluate',
        mode: 'helper',
        value: node.value,
        resolve: result => {
          resolve(coerceNumericOperandScalar(result));
        }
      });
      return;
    }
    if (mode === 'comparisonOperand') {
      let revivedValue;
      stack.push({
        kind: 'finalize',
        run: () => {
          resolve(getPipelineHelper('constant')(revivedValue));
        }
      });
      stack.push({
        kind: 'evaluate',
        mode: 'helper',
        value: node.value,
        resolve: result => {
          revivedValue = result;
        }
      });
      return;
    }
    if (mode === 'helper') {
      stack.push({
        kind: 'evaluate',
        mode,
        value: node.value,
        resolve
      });
      return;
    }
    let revivedValue;
    stack.push({
      kind: 'finalize',
      run: () => {
        resolve(getPipelineHelper('constant')(revivedValue));
      }
    });
    stack.push({
      kind: 'evaluate',
      mode,
      value: node.value,
      resolve: result => {
        revivedValue = result;
      }
    });
    return;
  }
  if (node.exprType === 'Variable' && typeof node.name === 'string') {
    resolve(getPipelineHelper('variable')(node.name));
    return;
  }
  if (typeof node.name === 'string') {
    const helperName = node.name;
    const args = Array.isArray(node.args) ? node.args : [];
    if ((helperName === 'scalar' || helperName === 'array') && args.length === 1 && nestedWebPipelineBuilder && nestedWebPipelineFirestore) {
      const nestedPipeline = extractNestedPipelineFromArg(args[0]);
      if (nestedPipeline) {
        const pipelineInstance = nestedWebPipelineBuilder(nestedWebPipelineFirestore, nestedPipeline);
        const subqueryMethod = helperName === 'scalar' ? pipelineInstance.toScalarExpression : pipelineInstance.toArrayExpression;
        if (typeof subqueryMethod !== 'function') {
          throw new Error('pipelineExecute() expected nested pipeline to support subquery expression conversion for web SDK.');
        }
        resolve(subqueryMethod.call(pipelineInstance));
        return;
      }
    }
    const revivedArgs = new Array(args.length);
    stack.push({
      kind: 'finalize',
      run: () => {
        if (helperName === 'array') {
          resolve(getPipelineHelper(helperName)(revivedArgs));
          return;
        }
        const finalizedArgs = finalizeArithmeticArgs(helperName, revivedArgs);
        if (helperName === 'divide' && isZeroNumericOperand(finalizedArgs[1])) {
          resolve(getPipelineHelper('constant')(0));
          return;
        }
        resolve(getPipelineHelper(helperName)(...finalizedArgs));
      }
    });
    for (let i = args.length - 1; i >= 0; i--) {
      const argMode = helperName === 'conditional' ? 'pipeline' : getArgReviveMode(helperName, i);
      stack.push({
        kind: 'evaluate',
        mode: argMode,
        value: args[i],
        resolve: result => {
          revivedArgs[i] = result;
        }
      });
    }
    return;
  }
  throw new Error('pipelineExecute() failed to rebuild a serialized expression node for web SDK.');
}
function rebuildAggregateNode(stack, node, resolve) {
  if (typeof node.kind !== 'string') {
    throw new Error('pipelineExecute() failed to rebuild aggregate node: missing aggregate kind.');
  }
  const helperName = node.kind;
  const args = Array.isArray(node.args) ? node.args : [];
  const revivedArgs = new Array(args.length);
  stack.push({
    kind: 'finalize',
    run: () => {
      resolve(getPipelineHelper(helperName)(...revivedArgs));
    }
  });
  for (let i = args.length - 1; i >= 0; i--) {
    stack.push({
      kind: 'evaluate',
      mode: 'helper',
      value: args[i],
      resolve: result => {
        revivedArgs[i] = result;
      }
    });
  }
}
function rebuildOrderingNode(stack, node, resolve) {
  if (!Object.prototype.hasOwnProperty.call(node, 'expr')) {
    throw new Error('pipelineExecute() failed to rebuild ordering node: missing expr.');
  }
  const direction = node.direction === 'descending' ? 'descending' : 'ascending';
  let revivedExpr;
  stack.push({
    kind: 'finalize',
    run: () => {
      resolve(getPipelineHelper(direction)(revivedExpr));
    }
  });
  stack.push({
    kind: 'evaluate',
    mode: 'pipeline',
    value: node.expr,
    resolve: result => {
      revivedExpr = result;
    }
  });
}
function rebuildAliasedNode(stack, node, nodeKey, resolve) {
  const alias = typeof node.alias === 'string' ? node.alias : undefined;
  if (!alias) {
    throw new Error(`pipelineExecute() failed to rebuild aliased node: missing ${nodeKey} alias.`);
  }
  let revivedValue;
  stack.push({
    kind: 'finalize',
    run: () => {
      resolve(applyAlias(revivedValue, alias, nodeKey));
    }
  });
  stack.push({
    kind: 'evaluate',
    mode: 'pipeline',
    value: node[nodeKey],
    resolve: result => {
      revivedValue = result;
    }
  });
}
function reviveValueWithMode(value, mode) {
  let finalValue;
  const stack = [{
    kind: 'evaluate',
    mode,
    value,
    resolve: result => {
      finalValue = result;
    }
  }];
  while (stack.length > 0) {
    const frame = stack.pop();
    if (frame.kind === 'finalize') {
      frame.run();
      continue;
    }
    const currentValue = frame.value;
    if (Array.isArray(currentValue)) {
      const revived = new Array(currentValue.length);
      frame.resolve(revived);
      for (let i = currentValue.length - 1; i >= 0; i--) {
        stack.push({
          kind: 'evaluate',
          mode: frame.mode,
          value: currentValue[i],
          resolve: result => {
            revived[i] = result;
          }
        });
      }
      continue;
    }
    if (!isRecord(currentValue)) {
      if (frame.mode === 'numericOperand') {
        frame.resolve(coerceNumericOperandScalar(currentValue));
        continue;
      }
      frame.resolve(currentValue);
      continue;
    }
    if (frame.mode === 'numericOperand' || frame.mode === 'comparisonOperand') {
      if (isExpressionNode(currentValue)) {
        rebuildExpressionNode(stack, currentValue, frame.mode, frame.resolve);
        continue;
      }
    }
    if (frame.mode === 'helper') {
      if (isFlatAliasedFieldNode(currentValue) || isAliasedExpressionNode(currentValue) || isAliasedAggregateNode(currentValue) || currentValue.__kind === 'aggregate' || currentValue.__kind === 'ordering') {
        stack.push({
          kind: 'evaluate',
          mode: 'pipeline',
          value: currentValue,
          resolve: frame.resolve
        });
        continue;
      }
      if (isExpressionNode(currentValue)) {
        rebuildExpressionNode(stack, currentValue, frame.mode, frame.resolve);
        continue;
      }
    } else {
      if (isFlatAliasedFieldNode(currentValue)) {
        frame.resolve(rebuildFlatAliasedField(currentValue));
        continue;
      }
      if (isAliasedExpressionNode(currentValue)) {
        rebuildAliasedNode(stack, currentValue, 'expr', frame.resolve);
        continue;
      }
      if (isAliasedAggregateNode(currentValue)) {
        rebuildAliasedNode(stack, currentValue, 'aggregate', frame.resolve);
        continue;
      }
      switch (currentValue.__kind) {
        case 'expression':
          rebuildExpressionNode(stack, currentValue, frame.mode, frame.resolve);
          continue;
        case 'aggregate':
          rebuildAggregateNode(stack, currentValue, frame.resolve);
          continue;
        case 'ordering':
          rebuildOrderingNode(stack, currentValue, frame.resolve);
          continue;
        case 'aliasedExpression':
          rebuildAliasedNode(stack, currentValue, 'expr', frame.resolve);
          continue;
        case 'aliasedAggregate':
          rebuildAliasedNode(stack, currentValue, 'aggregate', frame.resolve);
          continue;
        default:
      }
    }
    const revived = {};
    frame.resolve(revived);
    const entries = Object.entries(currentValue);
    for (let i = entries.length - 1; i >= 0; i--) {
      const [key, entry] = entries[i];
      stack.push({
        kind: 'evaluate',
        mode: frame.mode,
        value: entry,
        resolve: result => {
          revived[key] = result;
        }
      });
    }
  }
  return finalValue;
}
export function revivePipelineValue(value) {
  return reviveValueWithMode(value, 'pipeline');
}
//# sourceMappingURL=pipeline_node_builder.js.map