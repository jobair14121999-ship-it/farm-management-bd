import type { ReactNativeFirebase } from '@react-native-firebase/app';
import type { PhoneAuthSnapshot, PhoneAuthError } from './types/auth';
import type { AuthInternal } from './types/internal';
export default class PhoneAuthListener {
    private readonly _auth;
    private _reject;
    private _resolve;
    private _promise;
    private readonly _jsStack;
    private readonly _timeout;
    private readonly _phoneAuthRequestId;
    private readonly _forceResending;
    private readonly _internalEvents;
    private readonly _publicEvents;
    constructor(auth: AuthInternal, phoneNumber: string, timeout?: number, forceResend?: boolean);
    private _subscribeToEvents;
    private _addUserObserver;
    private _emitToObservers;
    private _emitToErrorCb;
    private _emitToSuccessCb;
    private _removeAllListeners;
    private _promiseDeferred;
    private _codeSentHandler;
    private _codeAutoRetrievalTimeoutHandler;
    private _verificationCompleteHandler;
    private _verificationFailedHandler;
    on(event: string, observer: (snapshot: PhoneAuthSnapshot) => void, errorCb?: (error: PhoneAuthError) => void, successCb?: (snapshot: PhoneAuthSnapshot) => void): PhoneAuthListener;
    then<TResult1 = PhoneAuthSnapshot, TResult2 = never>(onFulfilled?: ((value: PhoneAuthSnapshot) => TResult1 | PromiseLike<TResult1>) | null, onRejected?: ((reason: ReactNativeFirebase.NativeFirebaseError) => TResult2 | PromiseLike<TResult2>) | null): Promise<TResult1 | TResult2>;
    catch<TResult = never>(onRejected?: ((reason: ReactNativeFirebase.NativeFirebaseError) => TResult | PromiseLike<TResult>) | null): Promise<PhoneAuthSnapshot | TResult>;
}
//# sourceMappingURL=PhoneAuthListener.d.ts.map